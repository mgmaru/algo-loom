# JudgeAdapter技術検証記録: `<run-id>`

> 状態: 下書き
>
> 実施日: `<YYYY-MM-DD>`
>
> 検証計画: [`JudgeAdapter`技術検証計画](../../../project/judge-adapter-verification.md)
>
> 実施手順: [技術検証の実施手順と成果物](../README.md)

## ドキュメント概要

この記録は、`<run-id>`で観測した事実と合否を保持します。実際のアカウント名、Cookie、トークン、生のヘッダー、問題文、入出力例の実値、提出用ソースコード、生ログは含めません。

## 0. 結論

| 判定 | 結果 | 根拠 |
|---|---|---|
| P0 | `<合格 / 不合格 / 判定不能>` | `<V-01〜V-04、V-10の要約>` |
| P1 | `<合格 / 不合格 / 判定不能>` | `<V-05〜V-07、V-11の要約>` |
| P2 | `<観測済み / 一部観測 / 未観測>` | `<V-08〜V-09の要約>` |
| MVP実装開始条件1〜3 | `<充足 / 未充足>` | `<理由>` |
| `online-judge-tools` | `<採用候補 / 条件付き / 不採用 / 判断保留>` | `<CLIと下位APIを分けた理由>` |
| 方式Aの認証helper | `<採用候補 / 条件付き / 不採用 / 判断保留>` | `<V-10、V-11の根拠>` |

## 1. 実行条件

| 項目 | 観測値 |
|---|---|
| 実行ID | `<run-id>` |
| 開始・終了時刻 | `<UTC>` |
| 対象問題 | `<正規問題IDと公式URL>` |
| 終了済みの確認 | `<確認方法とUTC時刻>` |
| 対象コンテストの個別規則 | `<なし / あり。確認結果>` |
| 正規言語ID | `<canonical language ID>` |
| OS | `<製品名、バージョン、CPUアーキテクチャ>` |
| Python | `<バージョン>` |
| 実行入口 | `<oj / oj-api / Python API / 使い捨てラッパー>` |
| `online-judge-tools` | `<バージョン、取得元、配布ファイルのハッシュまたはリビジョン>` |
| `online-judge-api-client` | `<バージョン、取得元、配布ファイルのハッシュまたはリビジョン>` |
| 直接利用したHTTPライブラリ | `<名前とバージョン>` |
| 認証方式 | `<方式C / 方式A / 認証を扱わない実行>` |
| 認証browser・helper | `<方式Aの場合だけ、browser・version・起動条件・helper revision>` |
| session保管 | `<方式Cの一時領域 / 方式Aの検証用secret store。値と実pathは残さない>` |
| アカウント | `<識別情報を取得できた / できない、期待値と一致 / 不一致>` |
| ソースコード | `<source-A等の実行内別名、バイト数。本文とハッシュは保持しない>` |
| 実サービス提出の事前承認 | `<提出なし / 目的と対象を固定して最大1件を承認>` |
| 提出枠の状態 | `<未消費 / SEND_STARTED相当で1件消費。成功・失敗・不明を併記>` |

提出を伴う場合は、検証目的、対象問題、言語、支援コード、既存提出を再利用できない理由をこの節へ記録します。提出上限は日単位や検証全体の累積値ではなく、この明示承認済み実サービス検証実行で最大1件です。`SEND_STARTED`相当以降は結果にかかわらず枠を消費し、同じ承認で再提出しません。

## 2. 当日の外部条件

| 確認対象 | 確認時刻 | 結果 |
|---|---|---|
| AtCoder利用規約 | `<UTC>` | `<検証を妨げる変更の有無>` |
| 対象コンテストの公式ルール | `<UTC>` | `<終了状態と個別条件>` |
| AI学習拒否設定の公式案内 | `<UTC>` | `<提出前に案内した事実>` |
| AtCoderのBot対策 | `<UTC>` | `<通常手順で続行可能 / 停止>` |
| 候補ツールの公開情報 | `<UTC>` | `<確認したリリースと既知の問題>` |

## 3. 検証項目

状態は`未実施`、`合格`、`不合格`、`未観測`、`中止`から選びます。証拠は`実サービス`、`ローカル制御`、`静的確認`から選び、異なる証拠を一つの観測としてまとめません。

| ID | 優先度 | 状態 | 証拠 | 観測事実 | 欠損・制約 | 所要時間 |
|---|---|---|---|---|---|---|
| `V-01` | P0 | `<状態>` | `<証拠>` | `<件数と対応関係、取得元、時刻>` | `<欠損>` | `<ms>` |
| `V-02` | P0 | `<状態>` | `<証拠>` | `<認証状態と識別情報の取得可否>` | `<欠損>` | `<ms>` |
| `V-03` | P0 | `<状態>` | `<証拠>` | `<submission-Aの受理とID取得経路>` | `<欠損>` | `<ms>` |
| `V-04` | P0 | `<状態>` | `<証拠>` | `<判定待ちと最終判定、各取得時刻>` | `<欠損>` | `<ms>` |
| `V-05` | P1 | `<状態>` | `<証拠>` | `<候補数、解決後のID・表示名・処理系・バージョン>` | `<曖昧性>` | `<ms>` |
| `V-06` | P1 | `<状態>` | `<証拠>` | `<別プロセスからsubmission-Aを再照合した事実>` | `<欠損>` | `<ms>` |
| `V-07` | P1 | `<状態>` | `<証拠>` | `<分類できた原因と根拠>` | `<未観測の原因>` | `<ms>` |
| `V-08` | P2 | `<状態>` | `<証拠>` | `<実行時間・メモリの値、単位、または欠損>` | `<欠損>` | `<ms>` |
| `V-09` | P2 | `<状態>` | `<証拠>` | `<接続、リクエスト、ポーリングの上限と待機指示>` | `<未観測の条件>` | `<ms>` |
| `V-10` | P0 | `<状態>` | `<証拠>` | `<可視専用browser、人のlogin、必要Cookieだけの取得、同一account確認、既存profile非参照、cleanup>` | `<未検証OS・browser等>` | `<ms>` |
| `V-11` | P1 | `<状態>` | `<証拠>` | `<Set-Cookieの有無、更新、process再起動、expiry非推測、排他、失効分類>` | `<未観測の条件>` | `<ms>` |

## 4. 提出状態の観測

| 順序 | 状態 | UTC時刻 | 観測 |
|---:|---|---|---|
| 1 | `PREPARED`相当 | `<UTC>` | `<問題、アカウント、言語、ハッシュの確認>` |
| 2 | `SEND_STARTED`相当 | `<UTC>` | `<送信開始>` |
| 3 | `REMOTE_ACCEPTED`相当 | `<UTC>` | `<submission-Aの取得>` |
| 4 | `VERDICT_PENDING`相当 | `<UTC>` | `<判定待ち>` |
| 5 | `FINAL`相当 | `<UTC>` | `<最終判定>` |
| 6 | 別プロセスで再照合 | `<UTC>` | `<同じsubmission-A、追加提出なし>` |

到達しなかった状態は削除せず、`未到達`と理由を記録します。応答が不明な場合は`REMOTE_STATUS_UNKNOWN`相当の行を追加し、再提出していない事実を記録します。

## 5. リクエスト集計

| 操作 | 送信先 | メソッド | 回数 | 最小間隔 | 応答分類 | 再試行 | 備考 |
|---|---|---|---:|---:|---|---|---|
| 認証確認 | `<分類>` | `<GET等>` | `<n>` | `<ms>` | `<2xx / 4xx等>` | `<回数>` | `<待機指示の有無>` |
| 方式Aのlogin page | `AtCoder` | `<GET等>` | `<n>` | `<ms>` | `<分類>` | `0` | `<人がbrowserで操作。redirectを含む集計方法>` |
| 入出力例取得 | `<分類>` | `<GET等>` | `<n>` | `<ms>` | `<分類>` | `<回数>` | `<本文は保存していない>` |
| 言語解決 | `<分類>` | `<GET等>` | `<n>` | `<ms>` | `<分類>` | `<回数>` | `<候補数>` |
| 提出 | `<分類>` | `<GET / POST等>` | `<n>` | `<ms>` | `<分類>` | `<回数>` | `<明示承認済み実行内で最大1件。枠の消費状態>` |
| 判定確認 | `<分類>` | `<GET等>` | `<n>` | `<ms>` | `<分類>` | `<回数>` | `<ポーリング回数>` |
| 別プロセスの再照合 | `<分類>` | `<GET等>` | `<n>` | `<ms>` | `<分類>` | `<回数>` | `<再提出なし>` |
| AtCoder以外 | `<PyPI等>` | `<GET等>` | `<n>` | `<ms>` | `<分類>` | `<回数>` | `<AtCoder集計から分離>` |

## 6. 認証失敗の分類

| 原因 | 観測状態 | 証拠 | 区別に使えた情報 | 次の安全な行動 |
|---|---|---|---|---|
| 未認証 | `<観測済み / 未観測>` | `<証拠>` | `<許可リスト済み情報>` | `<行動>` |
| 期限切れ | `<観測済み / 未観測>` | `<証拠>` | `<許可リスト済み情報>` | `<行動>` |
| アカウント不一致 | `<観測済み / 未観測>` | `<証拠>` | `<一致 / 不一致だけ。実名を残さない>` | `<行動>` |
| AtCoder側の拒否 | `<観測済み / 未観測>` | `<証拠>` | `<許可リスト済み情報>` | `<行動>` |
| ページ構造の変更 | `<観測済み / 未観測>` | `<証拠>` | `<許可リスト済み情報>` | `<行動>` |
| 通信障害 | `<観測済み / 未観測>` | `<証拠>` | `<許可リスト済み情報>` | `<行動>` |
| secret store障害 | `<観測済み / 未観測>` | `<証拠>` | `<許可リスト済み情報>` | `<行動>` |

## 7. 未決事項へ渡す事実

### 7.1. 未決事項2.3の初期値候補

| 値 | 候補 | 根拠 | 決定前に追加で必要な検証 |
|---|---:|---|---|
| 接続上限 | `<ms>` | `<観測>` | `<追加検証>` |
| 1リクエスト上限 | `<ms>` | `<観測>` | `<追加検証>` |
| ポーリング間隔 | `<ms>` | `<観測>` | `<追加検証>` |
| ポーリング全体の上限 | `<ms>` | `<観測>` | `<追加検証>` |
| 再試行回数 | `<n>` | `<観測>` | `<追加検証>` |

### 7.2. 未決事項8.1の問い合わせ用事実

- 1回の検証でAtCoderへ送ったリクエスト数: `<n>`
- 操作別の間隔: `<要約>`
- AtCoderからローカルへ保存した内容: `<要約>`
- 保存しなかった内容: `<要約>`
- 使用した認証方式と認証情報の所有者: `<秘密情報を含まない要約>`
- 方式Cまたは方式Aで取り込んだCookieの範囲: `<REVEL_SESSIONだけ / 追加が必要になり停止>`
- 方式Aの専用profile、secret store、cleanup: `<秘密情報と実pathを含まない要約>`
- 提出回数: `1件以下`
- 429応答または待機指示: `<有無と対応>`
- Bot対策: `回避していない`

## 8. 設計への反映候補

観測事実と設計判断を分けます。この節の内容だけで正本文書を変更しません。

| 観測から生じた論点 | 影響する機能 | 判断する正本文書 | 次の作業 |
|---|---|---|---|
| `<論点>` | `<機能ID>` | `<文書>` | `<作業>` |

P0が不合格の場合は、回避実装ではなく次を記録します。

- 成立しなかったMVPの能力: `<能力>`
- 影響する価値仮説: `<仮説>`
- 再検討するMVP範囲: `<範囲>`
- 判断に必要な追加事実: `<事実>`

## 9. 成果物の機密情報検査

- [ ] Cookie、トークン、パスワード、CSRFトークンを含まない
- [ ] Cookie値の長さ、prefix、suffix、hash等、照合に利用できる派生値を含まない
- [ ] `Authorization`、`Cookie`、`Set-Cookie`等の生ヘッダーを含まない
- [ ] 問題文、生HTML、完全なJSON応答、入出力例の実値を含まない
- [ ] 提出用ソースコード、標準入力、標準出力、標準エラー出力を含まない
- [ ] 実際のアカウント名、メールアドレス、ホスト名、利用者名を含まない
- [ ] 不要な絶対パス、生ログ、完全なスタックトレースを含まない
- [ ] 実際の提出IDを残す場合は、必要性と公開範囲を確認した
- [ ] browser profile、keyring item、debug endpointの実pathまたは識別子を含まない
- [ ] `git diff --check`を通過した
- [ ] 一時データを破棄しても、この記録だけで合否の根拠を説明できる
